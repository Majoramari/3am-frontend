meta {
  name: 2. Delete Account (Destructive)
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/Account/delete
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("delete account is successful or already unauthorized", function () {
    const status = res.getStatus();
    expect([200, 201, 202, 204, 401, 403]).to.include(status);
  });
}
